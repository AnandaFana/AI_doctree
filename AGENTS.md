# 本工作区的开发边界

- 先读 README.md、docs/MARKDOWN_PROTOCOL_V2.md 和 docs/PROTOCOL.md，再改动协议或治理语义。v2 是当前 Markdown 目录路线；v1 文档和本地实验记录保留历史含义。
- 若本地存在 CODEX_HANDOFF.md 或原始实验记录，保留不覆盖。自有项目、副本、原始交接和私有配置不进入工具发布；不要操作来源项目远端。提交前阅读 docs/PUBLISHING.md，检查忽略规则、暂存内容和待推送历史。
- 扫描来源只读，不导入或执行来源 Python/脚本。变更节点协议时同步 scanner_version、格式说明和必要的迁移/回归。
- README 写入通过显式 plan_sync/apply_plan 完成：先生成预览，应用前核对全部输入版本，保留原文备份与写入日志。源码正文、冻结协议和历史记录不因导航同步而改写。
- v2 以项目 Markdown 为共享依据：身份与职责由人或 Agent 维护，父子导航由物理目录和最近的已接入祖先推导。完成本目录工作后更新本页；影响上级范围、结论或下一步时再核对父页。sync 不自动审阅或总结正文。
- 目录说明的治理范围由用户决定。接入先读取 coverage 的按层数量和缺失清单，结合项目用途建议深度；范围不明确时再询问。不得默认给每个实验批次或归档目录批量创建 README，也不得把未选范围的目录判为治理失败。范围明确后再预览与应用 cover；页面展开层数只控制展示，不授权文档写入。
- `.doctree/` 中的便携运行时代码只使用 Python 标准库，不依赖本工具旧版扫描器、治理库、PyYAML 或外部服务。不得在本地配置中保存唯一的共享职责、父子映射或项目结论。
- 源文件、候选推断、文档审阅和业务/科学验收分别表达；不能根据 exit code、模板汇总或任意自然语言自动升级验收。
- `.doctree/state.json` 含不可随意丢弃的审阅与事件记录。索引可重建，治理历史需要备份；保留失败产物和输入版本。
- 保留默认目录树与 `/details` 详细页入口；便携包只带树图与 Markdown 查看，不复制旧治理数据。安装器只维护声明过的工具文件及 AGENTS / .gitignore 有界区块，不清理未知文件。
- 教学项目可用于小型文档实验。v1 回放使用 scripts/demo_workflow.py 的默认临时副本模式；五节点 v2 回放使用 scripts/demo_v2_workflow.py，避免覆盖当前界面的工作历史。
- 本工具自身导航使用 scripts/sync_self.py --check / --apply，明确隔离独立样例、本地项目和产物。可选的 Git 文档核对见 docs/MAINTENANCE.md；人或 Agent 阅读实际差异并说明理由，只有声明影响直接父节点时才逐级处理，不自动改写所有祖先。
- 改完按风险运行相关 unittest；修改 UI 后用浏览器检查。不要执行接入项目的业务或科学计算来测试本工具。

# 版本说明

## 0.3.1 · 2026-09-13

- 补齐新电脑到新项目的安装步骤，明确工具部署、Agent 建立真实目录说明、结构检查和内容核对的完整流程；轻量路线无需 PyYAML。
- 提供可直接交给 Agent 的首次接入指令，随便携安装放入 `.doctree/ONBOARDING.md`，安装结果与共享 AGENTS 指向下一步；已安装项目可重装升级。
- 在 README 和 Agent 指令中加入 Mermaid 流程图，分别展示使用总览和范围选择、内容编写、验收分支。
- 便携 `sync` 新增可选 `--selections`，Agent 可一次注入有依据的职责和新文档正文；不带选择文件时仍只同步现有节点，既有正文和稳定 ID 保护不变。

验证：100/100 单元测试通过；另以全新目录按指南实际完成标准库安装、读取随包流程图、只读预览、根与子节点创建、范围检查、上下文导出和幂等重装。来源程序未执行或改写，范围外批次目录未创建说明。

## 0.3.0 · 2026-09-12

本次初始发布包含从详细治理原型到 Markdown 目录工具的三轮实现，主要用于本地试用和持续迭代。

### 面向人类的目录树

- 文件树为默认首页，画布占据主要空间，支持展开 1/2/3 层、逐节点折叠、搜索、平移和缩放。
- 点击节点阅读目录职责、README、父子入口，或打开本地文件夹。
- 保留详细治理页，并在右上角和左侧提供放大的返回目录树按钮。

### 面向 Agent 的共享说明

- Markdown 节点格式 schema 2 将身份、目录职责和生成导航分开；保留原有正文，支持自定义 Markdown 入口。
- `coverage` 报告目录分布与说明覆盖，`cover` 按显式深度或完整范围生成接入预览，`--apply` 才写入。
- 展示深度与治理范围独立；普通目录没有 README 时先作为待评估提示，不强制给所有产物目录建立文档。
- 同步核对输入版本和路径边界，保留写入备份与日志，并通过跨进程锁避免并发覆盖。

### 轻量部署与发布边界

- 可安装项目内 `.doctree/` 便携工具，仅依赖 Python 标准库；共享知识继续随 Markdown 和 Git 流转。
- 默认配置只提供两个教学样例；个人项目映射放在被忽略的 `config/projects.local.json` 中。
- 自有项目、试点记录、原始交接材料、缓存、备份和运行状态均不包含在发布文件中。
- 详细治理原型继续区分交付、集成、执行和验收，并保留版本核对、候选审阅与恢复能力。

### 验证与限制

发布内容的干净检出通过 **98/98 单元测试**，教学交接回放通过 **12/12 检查**；默认两个样例共 10 个节点扫描无警告，首页、详细页及树图/状态接口均返回 HTTP 200。测试涵盖协议、覆盖范围、治理状态、便携安装、输入冲突和配置选择；两个页面脚本语法检查通过。树图及双向页面导航也已在此前迭代中通过浏览器检查。

当前版本没有自动自然语言审阅、持续文件监听或远程协作服务；实际职责、摘要和验收仍需人或 Agent 核对。Python 3.10+ 是代码目标，当前实测环境为 Windows / Python 3.14.6；其他平台未作本轮实机验证。
